@using EPiServer.Core
@using Forte.EpiResponsivePicture.ResizedImage
@using ResponsivePictureSample.Features
@model EPiServer.Core.ContentReference

@if (!ContentReference.IsNullOrEmpty(Model))
{
    @Html.ResizedPicture(Model, PictureProfiles.XHtmlString)
}